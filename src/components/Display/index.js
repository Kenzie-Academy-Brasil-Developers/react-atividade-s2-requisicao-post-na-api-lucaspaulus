import React from 'react';
import * as S from "./style.js"
function Display({statusMsg}) {
  return(
            <S.Display>
                {statusMsg}
            </S.Display>
  )
}

export default Display;
